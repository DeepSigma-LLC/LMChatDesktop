<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LMChatDesktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LMChatDesktop"
    xmlns:models="using:LMChatDesktop.UI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LMChat">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <NavigationView x:Name="nvSample8" Grid.Row="1"
                    PaneDisplayMode="Left"
                    IsTabStop="False"
                    SelectionChanged="NavigationView_SelectionChanged"
                    MenuItemTemplateSelector="{StaticResource selector}"
                    MenuItemsSource="{x:Bind Categories, Mode=OneWay}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Frame Grid.Row="0" x:Name="contentFrame" />
                <TextBlock Grid.Row="1" Text="Version 1.0.0.0" HorizontalAlignment="Center" Padding="0,0,0,10" Foreground="Gray"/>
            </Grid>
        </NavigationView>
        
    </Grid>
    
</Window>
