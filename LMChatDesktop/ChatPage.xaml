<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LMChatDesktop.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LMChatDesktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Data template used: -->
        <!-- The data template is bound to a custom DataType called Message. -->
        <!-- Each Message object has a color and alignment assigned to it based on whether it was
sent or received, and those values are bound in the DataTemplate.-->
        <DataTemplate x:Key="MessageViewTemplate" x:DataType="local:Message">
            <Grid Height="Auto" Margin="4" HorizontalAlignment="{x:Bind MsgAlignment}">
                <StackPanel MinHeight="75" MaxWidth="600" Padding="10,0,0,10"
                            Background="{ThemeResource SystemControlForegroundChromeGrayBrush}"
                            CornerRadius="{StaticResource ControlCornerRadius}">
                    <TextBlock Text="{x:Bind MsgText}" Padding="0,10,0,0" FontSize="20" TextWrapping="WrapWholeWords"
                               Foreground="{ThemeResource SystemColorHighlightTextColor}"/>
                    <TextBlock Text="{x:Bind MsgDateTime}" Padding="0,0,0,10" FontSize="15"
                               Foreground="{ThemeResource SystemColorHighlightTextColor}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Fill space: ListView -->
            <RowDefinition Height="Auto"/>
            <!-- Fixed height: input bar -->
        </Grid.RowDefinitions>

        <!-- Chat list -->
        <ListView x:Name="InvertedListView"
              Grid.Row="0"
              ItemTemplate="{StaticResource MessageViewTemplate}"
              ScrollViewer.VerticalScrollMode="Auto"
              ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" VerticalAlignment="Bottom"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Input bar -->
        <Grid Grid.Row="1" Padding="8" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- TextBox expands -->
                <ColumnDefinition Width="Auto"/>
                <!-- Send button -->
                <ColumnDefinition Width="Auto"/>
                <!-- Clear button -->
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageInput" Grid.Column="0" TextWrapping="Wrap" PlaceholderText="Type your message here..." MaxHeight="160"/>
            <Button Grid.Column="1" Content="Send" Click="SendButton_Click" Style="{StaticResource AccentButtonStyle}"/>
            <Button Grid.Column="2" Content="Clear Chat" Click="ClearButton_Click"/>
        </Grid>
    </Grid>

</Page>
